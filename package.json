{
  "dependencies": {
    "leaflet": "^1.9.4",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate-zone-images": "node scripts/generate-zone-image-manifest.mjs",
    "save-manifest": "node save-manifest-server.js"
  }
}
